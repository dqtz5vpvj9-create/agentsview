name: In-session find publisher staging

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Patch payload staging branch"
